import OrganizeClient from "./OrganizeClient";

export const metadata = {
  title: "Organize Pages — SWFT PDF",
  description: "Reorder, rotate, and delete PDF pages with drag & drop. Everything runs in your browser.",
};

export default function OrganizePage() {
  return <OrganizeClient />;
}
